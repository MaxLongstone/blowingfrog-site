// An environmental effect in the stage before each boss, thrown in at random and
// meant to make that stage harder. The boss you are about to meet says a line as
// it starts. Only the first is built; the rest are planned in
// docs/frogpocalypse-backlog.md.
export const ENV = {
  '2': { id: 'powder', boss: 'chaco' },
};
